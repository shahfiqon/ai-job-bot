# Copy this file to .env and replace the placeholder values with your own secrets.
# Never commit the real .env file to source control.

# PostgreSQL connection string should mirror the docker-compose service definition.
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/jobbot

# Proxycurl API key is used by the CLI scrape command to enrich company profiles.
# The CLI automatically caches company data in the database to minimize API calls.
# Get your API key from: https://nubela.co/proxycurl
# Free tier: 10 credits/month, each company enrichment costs 1 credit
# Note: The scrape command checks the database first before calling the API
PROXYCURL_API_KEY=your_proxycurl_api_key_here
